
namespace ParallelParsing.GZTool;

public static class Utilities
{
	// public static string GiveMeSIUnits(ulong inputNumber, bool isBinary)
	// {
	// 	int i;
	// 	double number = (double)inputNumber;
	// 	double @base = isBinary ? 1024 : 1000;
	// 	char[] SIUnits = new { "", "k", "M", "G", "T", "P", "E" };
	// 	char[] SIBinaryUnits = new { "Bytes", "kiB", "MiB", "GiB", "TiB", "PiB", "EiB" };

	// 	for (i = 0; number >= @base; i++)
	// 	{
	// 		number /= @base;
	// 	}

	// 	return number.ToString() + isBinary ? SIBinaryUnits[i] : SIUnits[i];
	// }
}